# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

group :development, :test do
  gem 'base64' # ruby-mcp-client depends on it internally; Ruby >= 3.4 removed base64 from default gems
  gem 'dotenv', '~> 3.0'
  gem 'minitest', '~> 5.0'
  gem 'rake', '~> 13.0'
  gem 'rubocop', '~> 1.86'
  gem 'ruby-mcp-client', '~> 1.0'
  gem 'vcr', '~> 6.0'
  gem 'webmock', '~> 3.0'
end
